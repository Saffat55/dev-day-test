<style>
  /* ============================================================================
    Fonts
  ============================================================================== */

  /*
    @font-face {
      font-family: 'Font Family';
      src: url({{ 'font-family.woff' | asset_url }}) format('woff'),
        url({{ 'font-family.woff2' | asset_url }}) format('woff2');
      font-weight: 400;
      font-style: normal;
    }
  */
</style>